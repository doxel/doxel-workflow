PREFIX?=/usr/local
all:

install:
	install -D doxel-workerflow ${PREFIX}/bin/doxel-workerflow
	install -D loopback-login ${PREFIX}/bin/loopback-login
	install -D doxel-getjob ${PREFIX}/bin/doxel-getjob
	install -D doxel-segment-path ${PREFIX}/bin/doxel-segment-path
	install -D doxel-progress ${PREFIX}/bin/doxel-progress
	install -D doxel-segment-inject-pointcloud ${PREFIX}/bin/doxel-segment-inject-pointcloud

.PHONY: install all
